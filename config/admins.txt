# This file is a placeholder. The real admin list lives in the per-server
# config repo (https://github.com/ResurgenceStation/config) and is mounted
# at runtime into Configuration/GameStaticFiles/config/admins.txt by TGS.
#
# Format (one admin per line):
#   ckey = rank name
# where rank name is one of the ranks defined in admin_ranks.txt.
# Ckeys are lowercase with no punctuation except '@'.
#
# This stub exists so local builds and CI don't fail to open the file.
