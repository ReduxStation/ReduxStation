# This file is a placeholder. The real admin rank definitions live in the
# per-server config repo (https://github.com/ResurgenceStation/config) and
# are mounted at runtime into Configuration/GameStaticFiles/config/admin_ranks.txt
# by TGS.
#
# Format:
#   Name = Game Admin
#   Include = @ ADMIN BAN SOUND
#   Exclude = FUN
#   Edit =
#
# See the tgstation reference for the full keyword list.
# This stub exists so local builds and CI don't fail to open the file.
