author: s950tx16wasr10
delete-after: true
changes:
  - bugfix: 'admin Reboot World -> Regular Restart now produces fade-to-title and
      clients auto-reconnect, matching the working state from PR #86 before it was
      panic-reverted in PR #87.'
  - code_imp: restored upstream tgstation reboot pipeline (sync 
      TriggerRoundEndTgsEvent + addtimer-driven reboot_callback)
  - server: heavy AUDIT log_world instrumentation at every step of the reboot 
      chain, including the TGS Bridge REBOOT response body. Tagged 
      ``REBOOT_AUDIT:`` so it greps cleanly.
